# Title_Qwen_VL_25_7B dependencies (conda env: kardi_env, Python 3.10)
# Versions pinned from the working kardi_env install.

# --- torch: custom CUDA build, DO NOT install from plain PyPI ---
# Installed build is 2.11.0+cu130. `pip install torch` here will pull a
# different/incompatible wheel. Keep the existing build; do not reinstall.
# torch==2.11.0+cu130
# torchvision==0.26.0

# --- Qwen2.5-VL title-block extraction (core) ---
transformers==4.57.6
tokenizers==0.22.2
qwen-vl-utils==0.0.14
accelerate==1.13.0
numpy==2.2.6
pillow==12.0.0

# --- Notebooks (display) ---
ipython==8.38.0

# --- YOLO detectors (optional: train_obb.py / train_layout.py / notebook §7) ---
# Install ultralytics & its extras WITHOUT deps so they don't replace custom torch.
# Verified working set on kardi_env (ultralytics 8.4.67):
#   pip install --no-deps ultralytics ultralytics-thop seaborn polars
#   # already present in kardi_env: opencv-python, pyyaml, pandas, matplotlib,
#   #                               scipy, psutil, py-cpuinfo, tqdm, requests, numpy, pillow
# Note: ultralytics 8.4.67 uses `polars` for results.csv (required, else save crashes).
#   A non-fatal "PyDataFrame is not defined" plot warning may appear (cosmetic; weights OK).
opencv-python
pyyaml==6.0.2
pandas==2.3.3
matplotlib==3.10.8
